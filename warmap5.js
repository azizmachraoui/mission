/*******************************************1************************************************ */
// Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
/*******************************************2************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
/*******************************************3************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
/*******************************************4************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
/*******************************************5************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
/*******************************************6************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
/*******************************************7************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
/*******************************************8************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
/*******************************************9************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
/*******************************************10************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
/*******************************************11************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
/*******************************************12************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
/*******************************************13************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
/*******************************************14************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
/*******************************************15************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
/*******************************************16************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
/*******************************************17************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
/*******************************************18************************************************ */
// Style your code and explain it step by step before jumping to code 
// Read carefully, focus and test your code

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
/*******************************************19************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
/*******************************************20************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
/*******************************************21************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
/*******************************************22************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
/********************************************23************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
/*******************************************24************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
/*******************************************25************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
/*******************************************26************************************************ */
// Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
/*******************************************27************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
/*******************************************28************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
/*******************************************29************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
/*******************************************30************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
/*******************************************31************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
/*******************************************32************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
/*******************************************33************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
/*******************************************34************************************************ */
// Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
/*******************************************35************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
/*******************************************36********************************************************* */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
/*******************************************37************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
/*******************************************38************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
/*******************************************39************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
/*******************************************40************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
/*******************************************41************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
/*******************************************42************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
/*******************************************43************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
/*******************************************44************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
/*******************************************45************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
/*******************************************46************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
/*******************************************47************************************************ */
// Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
/*******************************************48************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
/*******************************************49************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
//*******************************************50************************************************ */
// Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
//*******************************************51************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
//*******************************************52************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
//*******************************************53************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
//*******************************************54************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
//*******************************************55************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
//*******************************************56************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
//*******************************************57************************************************ */
// Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
//*******************************************58************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
//*******************************************59************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
//*******************************************60************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
//*******************************************61************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
//*******************************************62************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
//*******************************************63************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
//*******************************************64************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
//*******************************************65************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
//*******************************************66************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
//*******************************************67************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
//*******************************************68************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
//*******************************************69************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
//*******************************************70************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
//*******************************************71************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
//*******************************************72************************************************ */
// Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
//*******************************************73************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
//*******************************************74************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
//*******************************************75************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
//*******************************************76************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
//*******************************************77************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
//*******************************************78************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
//*******************************************79************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
//*******************************************80************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
//*******************************************81************************************************ */
// Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
//*******************************************82************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
//*******************************************83************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
//*******************************************84************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
//*******************************************85************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
//*******************************************86************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
//*******************************************87************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
//*******************************************88************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
//*******************************************89************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
//*******************************************90************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
//*******************************************91************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
//*******************************************92************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
//*******************************************93************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
//*******************************************94************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
}
//*******************************************95************************************************ */
// Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
//*******************************************96************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
//*******************************************97************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
//*******************************************98************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
//*******************************************99************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }
/****************************************100************************************************ */
 // Style your code and explain it step by step before jumping to code 
 // Read carefully, focus and test your code 

 // 1- Using recursion Write a JavaScript function called (greatestCommonDivisor) to find the greatest common divisor (gcd) of two positive numbers.
 // Take into account handling when one of the inputs is not a number

function greatestCommonDivisor(num1, num2){
        if (typeof num1!=="number" || typeof num2!=="number"){
            return "enter valid number"}
        if (num2===0){
            return num1 }
        return greatestCommonDivisor(num1,num2%num1)
    }

 // 2-Write a function called (sum) that accepts two numbers as parameters, and sum them together but without summing them together directly, you can only add one at each summation, you'll need to use recursion in this.

function sum(x,y){
    if(y===0){
        return x}
    return sum(x+1,y-1)
    }

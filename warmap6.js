/************************************1************************************** */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }

/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
/************************************2************************************** */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
/************************************3************************************** */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
/************************************4************************************** */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
/************************************5************************************** */

// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
/************************************6************************************** */

// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
/************************************7************************************** */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
/************************************8************************************** */

// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
/************************************9************************************** */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
/************************************10************************************** */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
/************************************11************************************** */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
/************************************12************************************** */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
/************************************13************************************** */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
/************************************14************************************** */

// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
/************************************15************************************** */

// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
/************************************16************************************** */

// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
/************************************17************************************** */

// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
/************************************18************************************** */

// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
/************************************19************************************** */

// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
/************************************20************************************** */

// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
/************************************21************************************** */

// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
/************************************22************************************** */

// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
/************************************23************************************** */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
/************************************24************************************** */

// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************25************************************** */

// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************26************************************** */

// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************27************************************** */

// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************28************************************** */

// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************29************************************** */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************30************************************** */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************31************************************** */

// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************32************************************** */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************33************************************** */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************34************************************** */

// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************35************************************** */

// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************36************************************** */

// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************37************************************** */

// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************38************************************** */

// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************39************************************** */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************40************************************** */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************41************************************** */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************42************************************** */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************43************************************** */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************44************************************** */

// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************45************************************** */

// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;

}
//************************************46************************************** */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************47************************************** */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************48************************************** */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************49************************************** */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************50************************************ */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************51*********************************** */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************52*********************************** */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************53************************************* */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************54************************************* */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************55************************************* */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************56************************************* */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************57************************************* */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************58************************************* */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************59************************************* */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************60************************************* */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************61************************************ */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************62************************************* */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************63************************************* */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************64************************************* */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************65************************************ */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************66************************************* */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************67************************************* */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************68************************************* */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************69************************************* */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************70************************************ */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************71*********************************** */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************72*********************************** */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************73*********************************** */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************74*********************************** */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************75************************************* */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************76************************************ */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************77*********************************** */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************78*********************************** */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************79************************************ */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************80*********************************** */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************81********************************** */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************82********************************** */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************83********************************** */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************84********************************** */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************85********************************** */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************86********************************** */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************87********************************** */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************88********************************** */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************89********************************** */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************90********************************** */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************91************************************* */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************92************************************* */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************93************************************* */

// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************94************************************* */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************95************************************* */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************96************************************* */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************97************************************* */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************98************************************* */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************99************************************* */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}
//************************************100************************************* */
// 1-Create arrays in the global scope consisting of strings that represent:
// -name of females in your class
// -name of males in your class
// -name of your class instructors
var arrayoffeemales=["amira","mariem","eya"]
var arrayofmales=["aziz","jesser","hazem","bassem","mouhamed","sanad","azizmachraoui"]

/**
 * 2-write a function called (midElement) that takes an array as an argument and returns the element that is located in the middle of that array.
 * @param {Array<any>} array
 * @return {any}
 */
 function midElement(array) {
    var midIndex = Math.floor(array.length / 2);
    return array[midIndex];
 }


/**
 *
 * 3-write a function called (multipleEvenIndexes) that takes an array of numbers, as an argument and returns an array of numbers, multiplied by two for even indexes.
 *
 * @param {Array<number>} array
 * @return {Array<number>}
 */
function multipleEvenIndexes(array) {
    var result = [];
    for (var i = 0; i < array.length; i++) {
        if (i % 2 === 0) {
            result.push(array[i] * 2);
        } else {
            result.push(array[i]);
        }
    }
    return result;
}